<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="id.ac.umn.week10_28059">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Week10_28059">
        <service
            android:name=".TutorialC.CustomBoundService"
            android:enabled="true"
            android:exported="true"></service>
        <service
            android:name=".TutorialC.CustomService"
            android:enabled="true"
            android:exported="true" />
        <service android:name=".TutorialC.SimpleIntentService" />

        <activity android:name=".TutorialC.TutorialCMainActivity" />
        <activity android:name=".TutorialB.TutorialBMainActivity" />
        <activity android:name=".TutorialA.TutorialAMainActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>